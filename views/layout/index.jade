extend layout.jade

block content
    ul.product-grid
        each product in products
            li.item
                .image
                    img(src=product.thumbnail)
                label.name= product.name
                br
                label.price= "$" + product.price
